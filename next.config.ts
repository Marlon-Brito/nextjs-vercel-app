import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */

  // Dizer ao Next para compilar o projeto e o gerar de forma estática, jogando seu resultado dentro de uma pasta, para isso se usará "npm run build"
  // output: "export"
};

export default nextConfig;
